add_library(Render
  STATIC
  Render.hpp
  Render.cpp
)
if(${APPLE})
target_link_libraries(Render
  PRIVATE
  ${SDL2_LIBRARY}
  ${SDL2_IMAGE_LIBRARIES}
  Constants
  Player
  Object
  Sprite
  Enemy
  Projectile
  MapGenerator
  ImageLoader
)
else()
target_link_libraries(Render
  PRIVATE
  SDL2
  Constants
  Player
  Object
  Sprite
  Enemy
  Projectile
  ImageLoader
)
endif()

target_include_directories(Render
  PUBLIC
  .
)
