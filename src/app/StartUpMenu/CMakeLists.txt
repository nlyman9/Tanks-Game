add_library(StartUpMenu
  STATIC
  StartUpMenu.cpp
  StartUpMenu.hpp
)
if(${APPLE})
target_link_libraries(StartUpMenu
  PUBLIC
  GameLoop
  ${SDL2_LIBRARY}
  Render
  Args
  MultiplayerMenu
)
else()
target_link_libraries(StartUpMenu
  PUBLIC
  GameLoop
  SDL2
  Render
  Args
  MultiplayerMenu
)
endif()

target_include_directories(StartUpMenu
  PUBLIC
  .
)
