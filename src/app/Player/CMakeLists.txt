add_library(Player
  STATIC
  Player.hpp
  Player.cpp
)
if(${APPLE})
target_link_libraries(Player
  PRIVATE
  Object
  Constants
  Sprite
  ${SDL2_LIBRARY}
  Enemy
)
else()
target_link_libraries(Player
  PRIVATE
  Object
  Constants
  Sprite
  Controllers
)
endif()


target_include_directories(Player
  PUBLIC
  .
)
